http:
  routers:
    https-redirect:
      rule: "HostRegexp(`.*`)"
      entryPoints:
        - web
      middlewares:
        - redirect
      service: dummy

  services:
    dummy:
      loadBalancer:
        servers:
          - url: ""

  middlewares:
    redirect:
      redirectScheme:
        scheme: https
