http:
  routers:
    https-redirect:
      rule: "HostRegexp(`{any:.*}`)"
      entryPoints:
        - web
      middlewares:
        - redirect
      service: dummy

  services:
    dummy:
      loadBalancer:
        servers:
          - url: ""

  middlewares:
    redirect:
      redirectScheme:
        scheme: https
